interface:
  display_name: "Codex Watchdog"
  short_description: "Review stalls without interrupting active Codex work"
  default_prompt: "Use $codex-watchdog to diagnose this task without interrupting active work, then prefer safe same-task recovery."
